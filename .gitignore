# Generated launchd plists (regenerated by start-agents.sh)
launchd/

# Hermes machine state: secrets (.env), live SQLite (state.db), logs, caches.
# Lives in the repo so the whole experiment is one directory, but none of it
# is source — see hermes/plan.md.
hermes/data/
